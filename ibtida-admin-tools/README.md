# Ibtida Admin Tools

Admin-only scripts for Firestore seeding and maintenance. Run from project root or from this folder.

## Setup

1. Put your Firebase service account key in this folder as `serviceAccountKey.json` (do not commit it).
2. From project root: `cd ibtida-admin-tools && npm install`
